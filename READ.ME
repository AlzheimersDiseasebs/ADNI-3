ADNI-3 Data Set is included.

Images can be resized after downloading. Then the classification labels in the CSV file need to be matched with the images.

After matching the dataset with the CSV file, the models were run using 10 fold cross validation. And slice selection was made.
